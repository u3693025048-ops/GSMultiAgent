# T4 (工况4: 高频大幅度机动) 优化效率低根因分析

## 1. 问题概述

T4 子工况参数: 目标z向机动幅度 85 m/s, y向匀速横移 45 m/s, 机动频率 1.20 rad/s。
这是 T 类工况中难度最高的一档——目标做高频大过载机动。

**基线仿真结果 (来自 knowledge_base/monte mean results.md):**
- 命中率: 94.0%
- SEP: 4.22 m
- PeakNy: **51.53 g** (远超 20g 目标)
- PM: 77.1° (超过 70° 上限)
- BW: 6.31 rad/s (远低于 20 rad/s 下限)

基线有两个关键问题: PeakNy 严重超标 (2.5x), BW 严重不足 (1/3 下限)。

---

## 2. 五层根因分析

### 根因一 (核心问题): 参数调优 vs 结构改进的矛盾

**问题本质**: T4 的性能瓶颈是**制导律结构**而非参数选择。

当前 PPO 优化器可调参数 (9维):
```
w1, zeta1, tao1   — 俯仰通道驾驶仪参数
w2, zeta2, tao2   — 偏航通道驾驶仪参数
w3, zeta3         — 滚转通道驾驶仪参数
N_pn              — 比例导引系数
```

但 T4 的性能约束形成了**不可调和的三角矛盾**:

```
          峰值过载超标 (PeakNy→20g 以下)
                /\
               /  \
              /    \
             /      \
            /________\

  增大N_pn          减小N_pn
  (提高命中率       (降低PeakNy
   但PeakNy更高)    但命中率下降)

  提高w1/降低zeta1              降低w1/提高zeta1
  (提高BW, 降低PM               (改善PM, 但BW更低
   但PeakNy更高)                 命中率可能下降)
```

**具体分析**:

- **降低 PeakNy 需要减小 N_pn**: 但标准 PN 导引律在小 N_pn 时无法有效跟踪 1.2 rad/s 机动目标, 命中率急剧下降
- **提高 BW 需要增大 w1**: 但带宽增加会导致驾驶仪对噪声更敏感, 实际过载振荡加剧
- **修正 PM 需要降低 zeta1**: 但阻尼降低会让系统更接近不稳定, 实际飞行中容易失控

### 根因二: 20g 硬限幅引入的非线性

现有代码 gf() 中:
```matlab
N1 = max(-20, min(20, N1));
N2 = max(-20, min(20, N2));
```

标准 PN 下, 对于高频大机动目标, 导引律产生的过载指令远超 20g。硬限幅将指令"截断"在 20g, 但截断后的控制量无法补偿目标机动, 导致:

1. **非线性饱和效应**: 限幅器处于饱和状态时, 系统开环, 制导回路失效
2. **积分饱和 (windup)**: 驾驶仪中的积分项 cs.Ee_ay 在限幅期间持续累积, 解除限幅后产生过冲
3. **实际过载可能超过 20g**: cf() 中计算的是实际气动力 ayb, 由于驾驶仪动态特性, 可能 overshoot 超过 20g 指令值, 再加上重力补偿 G1*g0, 实测 PeakNy = 51.53g

### 根因三: 奖励函数信号噪声比低

_compute_reward() 中各分量在 T4 场景下的典型值:

| 分量          | 贡献范围       | 问题                                       |
|---------------|----------------|--------------------------------------------|
| hit_rate      | 0 ~ 3.0        | 在 94%~100% 区间变化小, 梯度信号弱        |
| SEP           | -2.0 ~ +4.0    | T4 下 SEP 在 4~8m, 信号尚可               |
| PeakNy 惩罚   | 0 ~ -9.4       | **主导项, 巨大负值**, 淹没其他信号        |
| PM bonus      | -1.5 ~ +1.5    | 在 [45,70] 范围内奖励相同, 无梯度引导    |
| BW bonus      | -1.2 ~ +0.8    | BW < 20 时持续惩罚, 但提高 BW 与降低 PeakNy 冲突 |

**关键问题**: PeakNy 惩罚在整个优化过程中是主导项 (50g → 惩罚约 -9.4)。但 RL 的 PPO 更新只能缓慢调整参数, 每个 episode 的 PeakNy 改善有限, 导致:

1. **奖励 landscape 陡峭且多峰**: 参数空间中, PeakNy 的突变区域 (限幅器切换点) 造成不连续
2. **梯度信号被噪声淹没**: Nmc=30 的蒙特卡洛评估噪声很大, 在 PeakNy≈50g 的区域, ±5g 的评估噪声完全淹没 ±2g 的参数梯度
3. **局部最优陷阱**: 一旦探索到某个"次优但稳定"的区域 (如 hit_rate=94%, PeakNy=45g), PPO 保守的策略更新 (clip_ratio=0.15) 无法跳出

### 根因四: AMRO 被禁用

配置中 amro_enabled: false:

```yaml
matlab_rl_optimizer:
  amro_enabled: false  # 禁用 AMRO, 使用标准 PPO 探索
```

AMRO (自适应多区域优化) 本可以:
- 检测参数空间的跳变区域 (如限幅器导致的奖励不连续)
- 标记禁区避免重复探索
- 在跳变边界执行大步长跳跃

在 T4 这种**不连续性强的场景**下禁用 AMRO, 标准 PPO 的局部梯度搜索效率极低。

### 根因五: 探索策略对强约束场景不适用

_init_networks() 中的探索噪声设计:
```python
_pitch_log_std = 0.0    # std ≈ 1.0  (俯仰通道大探索)
_other_log_std = -1.0   # std ≈ 0.37 (其他通道小探索)
```

这在宽度搜索 (exploration) 和深度搜索 (exploitation) 之间需要精细平衡。T4 的参数空间存在:
- **"悬崖"区域**: zeta1 < 0.35 时 PM 可能变负 (系统不稳定) → 仿真直接 crash
- **"高原"区域**: w1 > 50 且 N_pn > 6 时 PeakNy ≈ 50g 基本不变 → 无梯度信号
- **"窄谷"区域**: 同时满足 BW>20 且 PeakNy<25 的参数组合窗口极窄

标准 PPO 在悬崖附近过于保守 (clip_ratio=0.15), 在高原上无法获得有效梯度。

---

## 3. 历史运行证据

工作目录中存在多次 T4 优化尝试:
- guidance_T4_tune_* — 纯参数调优 (多次迭代, 始终不达标)
- guidance_T4_modlaw_* — 修改导引律尝试
- guidance_simulation_T4_APN_* — 增广比例导引 (APN) 尝试
- guidance_T4_limit_* — 修改限幅策略尝试
- guidance_T4_TUNE_PARAMS_* — 最新一轮 (仍为参数调优)

PE 记忆库中的最优 T4 结果:
```
命中率 100%, SEP=4.99m, PeakNy=22.25g, PM=61.4°, BW=70.18
```
PeakNy 仍超过 20g 目标, 说明参数优化已触及天花板。

---

## 4. 优化建议

### 建议一 (高优先级): 启用 AMRO 并调整参数

```yaml
matlab_rl_optimizer:
  amro_enabled: true
  amro_gradient_threshold: 0.3    # 更敏感
  amro_forbidden_zone_radius: 0.1 # 更精细的禁区
```

AMRO 可以检测到 20g 限幅器附近的奖励跳变, 避免在"悬崖"和"高原"区域浪费 episode。

### 建议二 (高优先级): 调整奖励函数权重

当前 T4 场景中 PeakNy 惩罚主导一切。建议:
```yaml
reward_weights:
  peak_ny_penalty: 1.0    # 从 2.5 降低
  hit_rate: 5.0            # 从 3.0 提高
  sep_low_bonus: 3.0       # 从 2.0 提高
```

降低 PeakNy 惩罚的压倒性优势, 让 RL 有足够空间在提高 BW 和降低 PeakNy 之间找到平衡。

### 建议三 (中优先级): 增加 Monte Carlo 评估次数

```yaml
matlab_rl_optimizer:
  nmc_per_eval: 50    # 从 30 提高
```

T4 工况的仿真结果随机性大 (目标高频机动 + 蒙特卡洛散布), Nmc=30 的评估噪声可能掩盖真实梯度。增加到 50 可提高信噪比。

### 建议四 (中优先级): 在奖励函数中增加过载的渐进式引导

当前 _compute_reward 对 PeakNy 只在超出 20g 时惩罚。建议增加渐进式引导:
```python
# 伪代码: 在 15-20g 区间增加温和鼓励, 引导 RL 主动降低 PeakNy
if 15 <= peak_ny <= 20:
    reward += 0.3 * (20 - peak_ny) / 5  # 线性 bonus
```

这样即使当前 PeakNy=50g, RL 也能从 49g→48g 的微小改善中获得正向奖励信号。

### 建议五 (低优先级, 根本性): 制导律结构改进

参数优化已触及天花板 (PE 最优 PeakNy=22.25g, 仍超 20g)。根本解决方案需要修改 gf() 制导律:

1. **引入过载管理策略**: 在 gf() 中增加 N_pn 的时变调度, 远距用大 N_pn 精确制导, 近距减小 N_pn 控制过载
2. **APN (增广比例导引)**: 增加目标加速度前馈补偿项, 减少 N_pn 的依赖, 从而降低 PeakNy
3. **过载约束最优导引律**: 在导引律中显式建模过载约束, 用最优控制求解

### 建议六 (低优先级): 分层优化策略

将 9 参数拆分为两组, 分阶段优化:
- **第一阶段** (BW/PM 优先): 固定 N_pn=4, 优化 w1/zeta1/tao1 使 BW>20 且 PM∈[45,70]
- **第二阶段** (过载/精度平衡): 固定驾驶仪参数, 精细调整 N_pn 使 PeakNy<20 同时命中率 ≥ 92%

分层优化可以避免 9 维空间中信号互相淹没的问题。

### 建议七 (低优先级): 增加探索噪声衰减策略

当前各维度探索噪声固定不变。建议加入衰减:
```python
# 前期 (ep 1-20): 大探索, 找到可能区域
# 中期 (ep 21-50): 中探索, 精细调优
# 后期 (ep 51+): 小探索, 收敛到最优
decay_factor = max(0.1, 1.0 - ep / max_ep)
self._actor.log_std[i] = base_log_std * decay_factor
```

---

## 5. 总结

| 问题层面 | 严重程度 | 可能改善幅度 | 建议优先级 |
|----------|----------|-------------|-----------|
| 奖励函数 PeakNy 惩罚过强 | ⭐⭐⭐⭐⭐ | 中等 (10-20% 效率) | 建议二 |
| AMRO 被禁用 | ⭐⭐⭐⭐ | 中等 (15-25% 效率) | 建议一 |
| Nmc 评估噪声大 | ⭐⭐⭐ | 中等 (改善梯度质量) | 建议三 |
| 缺少渐进式过载引导 | ⭐⭐⭐ | 中等 (改善探索效率) | 建议四 |
| 参数 tuning 已触及天花板 | ⭐⭐⭐⭐⭐ | 大 (需配合制导律修改) | 建议五 |
| 9 维空间信号淹没 | ⭐⭐⭐ | 中等 (拆分后更可控) | 建议六 |
| 固定探索噪声 | ⭐⭐ | 小 | 建议七 |

**核心结论**: T4 优化低效的根因是参数调优无法解决的**结构性问题** — 标准比例导引面对 1.2 rad/s / 85m/s 高频大机动目标时, 20g 过载硬限幅使系统物理上无法同时满足命中精度和过载约束。PPO 探索空间的有效区域极为狭窄, 加上奖励函数中 PeakNy 惩罚过强导致其他指标的梯度信号被淹没, 形成了优化瓶颈。

建议优先实施方案一(AMRO)和二(调整奖励权重)以短期提升效率, 方案五(制导律结构改进)才是长期根本出路。
